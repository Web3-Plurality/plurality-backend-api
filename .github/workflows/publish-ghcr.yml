name: Docker Image CI (Github Registry)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build and Push the image to ghcr
      run: |
        docker build . --file Dockerfile --tag ghcr.io/pluralitydev/plurality-backend-api-ghcr:latest
        docker login --username ${{ secrets.GHCR_USERNAME }} --password ${{ secrets.GHCR_TOKEN }}
        docker push ghcr.io/pluralitydev/plurality-backend-api-ghcr:latest
